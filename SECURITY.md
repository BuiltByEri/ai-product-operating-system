# Security

This repository should not contain secrets, tokens, credentials, local machine paths, production data, client-specific artifacts, or non-public source material.

Before publication or release changes, run the safety scan, link validation, version validation, skill validation, and manual review. If sensitive material is found, remove it from the release branch and restart validation before publication.

Security-sensitive findings should be handled outside public issues when disclosure could expose sensitive information.
